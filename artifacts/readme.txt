This folder should contain two binary files:

    model.pickle
    processing_parameters.pickle

However, those files are not committed/pushed to the repository.
Instead, they should be either

1. generated during training, or
2. downloaded from an external service.